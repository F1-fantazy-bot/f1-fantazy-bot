{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "hand-authored",
      "comment": "ARM template for the F1 Fantasy AGENT web frontend (Static Web App). Provisions a Free-tier SWA in westeurope with no GitHub repo linkage — the deployment token is fetched out-of-band (`az staticwebapp secrets list`) and pasted into the GH secret AZURE_STATIC_WEB_APPS_API_TOKEN. The hand-written GH workflow then uses Azure/static-web-apps-deploy@v1, which gives us full control over the build command (so VITE_AGENT_API_URL can be baked in per-environment) and matches the rest of the repo's deploy conventions."
    }
  },
  "parameters": {
    "staticWebAppName": {
      "type": "string",
      "defaultValue": "f1-fantazy-agent-web",
      "metadata": {
        "description": "Name of the Static Web App resource."
      }
    },
    "location": {
      "type": "string",
      "defaultValue": "westeurope",
      "allowedValues": ["centralus", "eastus2", "eastasia", "westeurope", "westus2"],
      "metadata": {
        "description": "Azure region. SWA Free SKU is only available in this short list of regions; westeurope keeps it co-located with the rest of the resource group."
      }
    },
    "sku": {
      "type": "string",
      "defaultValue": "Free",
      "allowedValues": ["Free", "Standard"],
      "metadata": {
        "description": "SKU tier. Free is sufficient for v1 (100 GB bandwidth/month, 0.5 GB storage, 100 staging environments — more than enough for PR previews)."
      }
    },
    "stagingEnvironmentPolicy": {
      "type": "string",
      "defaultValue": "Enabled",
      "allowedValues": ["Enabled", "Disabled"],
      "metadata": {
        "description": "Whether per-PR preview environments are enabled. We rely on this for PR test deploys, so it must stay Enabled."
      }
    },
    "customDomains": {
      "type": "array",
      "defaultValue": [],
      "metadata": {
        "description": "List of custom domains (e.g. ['f1.kilzid.com']) to attach to the SWA. Each one is provisioned as a `Microsoft.Web/staticSites/customDomains` child resource. DNS validation prerequisite: each domain MUST already have a CNAME record pointing at the SWA's defaultHostname (e.g. f1.kilzid.com CNAME calm-beach-055be4603.7.azurestaticapps.net) BEFORE this template is applied for the first time, otherwise the deployment fails with validation timeout (~10 min). After validation succeeds Azure auto-issues a managed TLS cert (~5-15 min). Reapplying when a domain already exists is a no-op."
      }
    }
  },
  "resources": [
    {
      "comments": "Static Web App. No repositoryUrl/branch/repositoryToken — the GH workflow drives deploys directly via Azure/static-web-apps-deploy@v1 using the deployment token (apiKey) that this resource auto-generates.",
      "type": "Microsoft.Web/staticSites",
      "apiVersion": "2023-12-01",
      "name": "[parameters('staticWebAppName')]",
      "location": "[parameters('location')]",
      "sku": {
        "name": "[parameters('sku')]",
        "tier": "[parameters('sku')]"
      },
      "properties": {
        "stagingEnvironmentPolicy": "[parameters('stagingEnvironmentPolicy')]",
        "allowConfigFileUpdates": true,
        "provider": "Custom",
        "enterpriseGradeCdnStatus": "Disabled"
      }
    },
    {
      "comments": "Custom-domain child resources. One per entry in the `customDomains` parameter. Each requires a pre-existing CNAME (or A/ALIAS) record at the user's DNS provider pointing at the SWA's defaultHostname. Provisioning blocks until DNS validation succeeds — if DNS isn't ready, the whole template deployment will eventually fail (~10 min timeout) and you'll need to retry after fixing DNS. The validationMethod defaults to `cname-delegation` which is correct for Free SKU.",
      "condition": "[greater(length(parameters('customDomains')), 0)]",
      "type": "Microsoft.Web/staticSites/customDomains",
      "apiVersion": "2023-12-01",
      "name": "[concat(parameters('staticWebAppName'), '/', parameters('customDomains')[copyIndex()])]",
      "dependsOn": [
        "[resourceId('Microsoft.Web/staticSites', parameters('staticWebAppName'))]"
      ],
      "properties": {},
      "copy": {
        "name": "customDomainsLoop",
        "count": "[length(parameters('customDomains'))]"
      }
    }
  ],
  "outputs": {
    "staticWebAppName": {
      "type": "string",
      "value": "[parameters('staticWebAppName')]"
    },
    "staticWebAppDefaultHostName": {
      "type": "string",
      "value": "[reference(resourceId('Microsoft.Web/staticSites', parameters('staticWebAppName')), '2023-12-01').defaultHostname]"
    },
    "staticWebAppResourceId": {
      "type": "string",
      "value": "[resourceId('Microsoft.Web/staticSites', parameters('staticWebAppName'))]"
    }
  }
}
